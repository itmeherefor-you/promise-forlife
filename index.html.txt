<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ma Question pour toi... ❤️</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            background: linear-gradient(to bottom right, #ffafcc, #ff4d6d);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            overflow: hidden;
            color: #590d22;
        }
        .container {
            background: rgba(255, 255, 255, 0.9);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
        }
        .buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }
        button {
            padding: 15px 40px;
            font-size: 1.5rem;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
        }
        #btnOui {
            background-color: #38b000;
            color: white;
        }
        #btnNon {
            background-color: #e63946;
            color: white;
            position: relative; /* Important pour l'animation JS */
        }
        #btnOui:hover { transform: scale(1.1); }
        .hidden { display: none !important; }
        #pageOui {
            font-size: 3rem;
        }
        .heart {
            font-size: 4rem;
            animation: pulse 1s infinite;
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.3); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body>
    <!-- PAGE 1 : La Question -->
    <div id="pageQuestion" class="container">
        <h1>Mon cœur, veux-tu être mon Valentine cette année ? 🌹</h1>
        <div class="buttons">
            <button id="btnOui">OUI ! Mille fois OUI ! ❤️</button>
            <button id="btnNon">Euh... Non ?</button>
        </div>
    </div>

    <!-- PAGE 2 : Réaction OUI -->
    <div id="pageOui" class="container hidden">
        <div class="heart">🥳</div>
        <h1>GÉNIAL ! TU VAS ME RENDRE LE PLUS HEUREUX DES HOMMES/FILLES !</h1>
        <p>Je t'aime plus que tout. On va passer une superbe soirée ! 🥂<br><small>Ferme les yeux le 14, j'ai une surprise...</small></p>
    </div>

    <script>
        // Récupérer les éléments
        const pageQuestion = document.getElementById('pageQuestion');
        const pageOui = document.getElementById('pageOui');
        const btnOui = document.getElementById('btnOui');
        const btnNon = document.getElementById('btnNon');

        // --- Gestion du bouton OUI ---
        btnOui.addEventListener('click', function() {
            pageQuestion.classList.add('hidden');
            pageOui.classList.remove('hidden');
            // Lance des confettis si tu veux ! (Cherche "canvas-confetti" sur le web)
        });

        // --- Gestion du bouton NON ESPIÈGLE ---
        let clickCount = 0;
        const messagesNon = ["Sûr ?", "Vraiment ?", "Réfléchis bien...", "Dernière chance !", "Tu es têtu(e) !", "Bon, okay... 😢"];

        btnNon.addEventListener('click', function() {
            // Faire bouger le bouton aléatoirement
            const x = Math.random() * (window.innerWidth - btnNon.offsetWidth);
            const y = Math.random() * (window.innerHeight - btnNon.offsetHeight);

            btnNon.style.position = 'fixed';
            btnNon.style.left = x + 'px';
            btnNon.style.top = y + 'px';

            // Changer le texte
            if (clickCount < messagesNon.length) {
                btnNon.textContent = messagesNon[clickCount];
            } else {
                // Après plusieurs clics, forcer le OUI
                pageQuestion.classList.add('hidden');
                pageOui.classList.remove('hidden');
            }

            // Réduire sa taille
            const currentSize = 1.5 - (clickCount * 0.1);
            btnNon.style.fontSize = currentSize + 'rem';

            clickCount++;
        });

        // Empêcher de sortir de l'écran
        btnNon.addEventListener('mouseover', function() {
            // Petit effet supplémentaire : bouge un peu au survol aussi
            if (clickCount > 2) {
                const x = Math.random() * (window.innerWidth - btnNon.offsetWidth);
                const y = Math.random() * (window.innerHeight - btnNon.offsetHeight);
                btnNon.style.left = x + 'px';
                btnNon.style.top = y + 'px';
            }
        });
    </script>
</body>
</html>